All files in the directory "/ai_models/llama/rag/docs" must be Markdown files (.md).
The script in document_indexer.py will transform each document into a vectorized version for the AI model.

"Human Docs" are not to make chunks, AIs need a chunkable version (compact and less narrative)